# TAX---The-most-agressive-compressing-software-in-the-world
TAX is the most agressive compressing software in the world. Guaranteed.

# Setup
To set up TAX, in the project root directory run the `make` command.

# Usage
The usage is *EXACTLY* the same as that of `tar`, and TAX simply makes it far more agressive, without any issues ;)
